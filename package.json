{
  "name": "imgur-searchbar",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "client:dev": "yarn workspace client start",
    "server:dev": "yarn workspace server start",
    "dev": "run-p server:dev client:dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
